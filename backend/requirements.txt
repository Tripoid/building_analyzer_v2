# Backend dependencies for Facade Analyzer
# Install with: pip install -r requirements.txt

# Web framework
fastapi==0.115.*
uvicorn[standard]==0.34.*
python-multipart==0.0.*

# ML / Computer Vision
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.40.0
accelerate>=0.30.0

# Segmentation (HuggingFace: Grounding DINO + SAM)
rembg[cpu]>=2.0.50

# Inpainting — LaMa (Large Mask Inpainting).
# Replaces Stable Diffusion: faster, no 512-px limit, preserves facade textures.
# Downloads ~200 MB model weights automatically on first inference.
simple-lama-inpainting>=0.1.2

# Image processing
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
