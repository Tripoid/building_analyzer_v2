# Backend dependencies for Facade Analyzer
# Install with: pip install -r requirements.txt

# Web framework
fastapi==0.115.*
uvicorn[standard]==0.34.*
python-multipart==0.0.*

# ML / Computer Vision
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.40.0
accelerate>=0.30.0

# Segmentation
rembg[cpu]>=2.0.50
# CLIPSeg — texture/material heatmaps (CIDAS/clipseg-rd64-refined via HuggingFace)
# Included in transformers>=4.18; no extra install needed.
# SAM2 — installed separately: pip install git+https://github.com/facebookresearch/sam2.git

# Inpainting — LaMa (Large Mask Inpainting).
# Replaces Stable Diffusion: faster, no 512-px limit, preserves facade textures.
# Downloads ~200 MB model weights automatically on first inference.
simple-lama-inpainting>=0.1.2

# Image processing
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=9.0.0

# Price updater (leroymerlin.ru scraper)
requests>=2.28.0
beautifulsoup4>=4.12.0
